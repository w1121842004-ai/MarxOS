# MarxOS 开发日志
2026-05-18

---

## 1. 读取 5.17 日志后的任务

5.17 下一步要求先重跑 `mea01.pdf` 的局部 OCR，确认新版 cache 是否稳定生成：

- `layout_lines`
- `header_text`
- `footer_text`
- `page_number_candidates`
- `page_number_source=ocr_layout`

今天先按小范围样本处理 `mea01.pdf` 第 180-230 PDF 页。

## 2. OCR 小样本结果

第一次只设置 `OVERWRITE_OCR=1` 时，脚本仍优先使用 PDF 文本层，因此不会生成 layout 字段。随后强制走 PaddleOCR 重跑第 180-230 页。

审计结果：

- 51 页全部生成 `layout_lines/header_text/body_text/footer_text/page_number_candidates` 字段。
- 48 页识别到页脚页码。
- 页码关系稳定：`PDF 180 -> 印刷页 159`，之后基本按 PDF 页递增。
- 未直接识别候选页码的页为 `204`、`206`、`220`，页脚分别类似 `18.3`、`18.5`、`19`，属于规则有意拒绝的正文/注释式数字。

## 3. 页码连续性修正

确认新的情况：部分图片页页脚没有可置信页码，但前后文本页的印刷页码连续，中间缺口应按同一 source 的连续页码顺延。

今天在 `rag/build_vectorstore_from_cache.py` 增加：

- `infer_page_from_sequence()`
- 短缺口顺延规则：同一 source 内，若前一可信页码存在，且 PDF 页差在 1-3 页内，则用 `previous_printed_page + gap` 补齐。
- 补齐后的 metadata 标记 `page_number_source=page_sequence`。

验证样例：

```text
203 182 printed_page ocr_layout
204 183 printed_page page_sequence
205 184 printed_page ocr_layout

218 197 printed_page ocr_layout
219 198 printed_page ocr_layout
220 199 printed_page page_sequence
221 200 printed_page ocr_layout
222 201 printed_page ocr_layout
```

## 4. OCR 强制开关

在 `rag/ocr_to_cache.py` 增加：

```powershell
$env:FORCE_OCR="1"
```

该开关会忽略可用 PDF 文本层，直接运行 PaddleOCR，避免以后为了生成 layout 字段而临时调大 `TEXT_LAYER_MIN_LENGTH`。

## 5. 验证

```text
venv\Scripts\python.exe -m unittest tests.test_page_metadata_inference tests.test_app_local_paths
Ran 28 tests
OK

venv\Scripts\python.exe -m py_compile rag\ocr_to_cache.py rag\build_vectorstore_from_cache.py tests\test_page_metadata_inference.py
OK
```

## 6. 下一步

原先计划批量强制重跑 `mea01.pdf` - `mea10.pdf`。今天实际验证后修正判断：当前目标是准确识别 `printed_page`，不是重新生产全文 OCR。因此不需要全量 OCR。

更合适的流程是：

1. 用现有 cache 扫描页码序列。
2. 直接接受稳定连续的 `ocr_layout` 页码。
3. 对短缺口使用 `page_sequence` 顺延。
4. 对稳定序列中突然跳开的候选页码使用 `page_sequence_corrected` 覆盖。
5. 只对剩余断点附近做局部 OCR 或人工复核。

新增轻量审计脚本：

```powershell
venv\Scripts\python.exe scripts\audit_cache_page_sequence.py --sources mea01.pdf,mea02.pdf
```

当前审计结果：

```text
mea01.pdf: docs=903 printed=896 pdf_only=7 sequence_breaks=4
mea02.pdf: docs=886 printed=880 pdf_only=6 sequence_breaks=6
```

后续根据“题名页/空白页不应延续上一篇页码序列”的判断，又做了两处修正：

- `document_from_cache()` 遇到 `title_page` 时重置当前 source 的页码连续上下文。
- `scripts/audit_cache_page_sequence.py` 区分普通断点与可由中间题名页、空白页、目录页解释的自然断点。

这避免了跨新著作封面把旧序列强行顺延到新著作中。

对 `mea01.pdf` - `mea10.pdf` 的轻量审计显示：

```text
mea01.pdf: printed=895 pdf_only=8 sequence_breaks=8 explained=2
mea02.pdf: printed=879 pdf_only=7 sequence_breaks=7 explained=2
mea03.pdf: printed=330 pdf_only=392 sequence_breaks=14
mea04.pdf: printed=280 pdf_only=406 sequence_breaks=8
mea05.pdf: printed=840 pdf_only=154 sequence_breaks=1
mea06.pdf: printed=388 pdf_only=215 sequence_breaks=5
mea07.pdf: printed=922 pdf_only=162 sequence_breaks=0
mea08.pdf: printed=355 pdf_only=281 sequence_breaks=11
mea09.pdf: printed=387 pdf_only=271 sequence_breaks=6
mea10.pdf: printed=349 pdf_only=585 sequence_breaks=14
```

解释：

- `mea01/02/07` 已经基本进入可用状态。
- `mea05` 只有 1 个主要断点，优先级较低。
- `mea03/04/08/10` 的旧 cache 中 `text_margin` 比例较高，需要围绕断点窗口补少量 layout 锚点，而不是全量 OCR。
- `mea10` 是书信卷，文章短、题名页密集，页码序列天然比长篇著作更碎，后续要用更局部的文章段落锚点判断。

随后对 `mea03.pdf` 采用“断点两端补锚点”策略，而不是整段 OCR：

- 对 14 个断点的前后端点各取约 5 页强制 OCR。
- 共补约 110 页 layout cache。
- 重新审计后：

```text
mea03.pdf: docs=728 printed=370 pdf_only=358
page_number_source: ocr_layout=86 page_sequence=270 page_sequence_corrected=11 text_margin=361
sequence_breaks=1 explained=title_boundary=7
```

剩余唯一断点位于 `pdf 816-830` 的报刊索引/名目索引区。该区大量数字是索引条目页码，不应按正文 `printed_page` 逻辑强行识别，因此暂不继续 OCR。

继续按同一策略处理 `mea04/05/06/08/09/10`：

- 先从审计断点生成两端小窗口。
- 只对窗口端点附近约 5 页强制 OCR。
- 共补约 120 页 layout cache。
- 重新审计 `mea01.pdf` - `mea10.pdf`。

复审结果：

```text
mea01.pdf: sequence_breaks=4, explained=title_boundary=6
mea02.pdf: sequence_breaks=0, explained=title_boundary=9
mea03.pdf: sequence_breaks=1, explained=title_boundary=7
mea04.pdf: sequence_breaks=1, explained=title_boundary=3
mea05.pdf: sequence_breaks=1
mea06.pdf: sequence_breaks=1, explained=title_boundary=1
mea07.pdf: sequence_breaks=0
mea08.pdf: sequence_breaks=1, explained=title_boundary=4
mea09.pdf: sequence_breaks=2, explained=title_boundary=3
mea10.pdf: sequence_breaks=0, explained=title_boundary=9
```

剩余未解释断点主要落在：

- 卷首说明或早期前置材料。
- 题名页附近的新篇边界。
- 卷末报刊索引、名目索引、文献索引区域。

这些区域数字密集，很多数字本身是索引项、年份、注释页码或书目页码，不应继续用正文 `printed_page` 逻辑强拉。当前可进入重建向量库阶段。

下一步应继续跑：

```powershell
venv\Scripts\python.exe scripts\audit_cache_page_sequence.py --sources mea03.pdf,mea04.pdf,mea05.pdf,mea06.pdf,mea07.pdf,mea08.pdf,mea09.pdf,mea10.pdf
```

等 `mea01` - `mea10` 的断点窗口确认后，再重建 `vectorstore/marx_reader_core`，并运行页码审计和检索评估。
## 7. `mes01` - `mes04` 页码审计

用户询问 `mes01` - `mes04` 的 `printed_page` 情况后，补做轻量审计。

结果：

```text
mes01.pdf: trusted body citable = 98.0%, sequence_breaks=0
mes02.pdf: trusted body citable = 99.3%, sequence_breaks=0
mes03.pdf: trusted body citable = 99.3%, sequence_breaks=0
mes04.pdf: trusted body citable = 98.8%, sequence_breaks=0
```

解释：
- 四本《马克思恩格斯选集》卷的正文页码序列稳定。
- 剩余未用 `printed_page` 的页主要是题名页、目录、索引、说明页等自然非正文区域。
- 可进入向量库重建，不需要全量 OCR。

## 8. 重建核心向量库

按当前稳定范围重建：

```powershell
$targets='mea01.pdf,mea02.pdf,mea03.pdf,mea04.pdf,mea05.pdf,mea06.pdf,mea07.pdf,mea08.pdf,mea09.pdf,mea10.pdf,mes01.pdf,mes02.pdf,mes03.pdf,mes04.pdf'
$env:TARGET_PDFS=$targets
$env:SKIP_PDFS=''
$env:VECTORSTORE_DIR='vectorstore/marx_reader_core'
$env:ARTICLE_MAP_PATH='rag/article_map_core.json'
$env:BATCH_SIZE='1024'
$env:READ_PROGRESS_EVERY='5000'
$env:SPLIT_PROGRESS_EVERY='5000'
$env:PYTHONIOENCODING='utf-8'
venv\Scripts\python.exe -u rag\build_vectorstore_from_cache.py
```

结果：

```text
valid pages: 12361
chunks: 42046
vectorstore: vectorstore/marx_reader_core
```

重建后运行 metadata 审计：

```text
documents: 42046
sources: 14
printed_page docs: 28500
pdf-only docs: 13546
suspicious printed-page rows: 39
```

可疑行主要集中在卷首、题名页、索引、短文章边界，不影响正文主检索。

## 9. 检索评测与精确引文排序修复

运行 `scripts/evaluate_retrieval.py` 后，核心书目查询、精确引文查询、负例抑制整体通过。

发现一个重要排序问题：

```text
“全世界无产者，联合起来！”出自哪里？
```

短引文会先命中《共产党宣言》的口号题名页或序言引用页，而不是正文结尾页。

修复：
- 在 `rag/exact_quote_lookup.py` 增加该口号到 `communist_manifesto` 的偏好映射。
- 为 exact quote 命中增加 `hit_quality_rank()`：
  - 正文结尾语境优先。
  - 孤立短标题页降权。
  - 序言、说明、索引、目录、注释页降权。
  - 保留 core classic 的 primary/parallel priority。
- 增加单元测试：
  - `test_exact_quote_prefers_manifesto_for_workers_of_world_slogan`

修复后手工验证命中顺序：

```text
1 mes01.pdf 451 communist_manifesto primary
2 mea02.pdf 86 communist_manifesto parallel
3 mea02.pdf 15 communist_manifesto parallel
4 mes01.pdf 409 communist_manifesto primary
5 mea02.pdf 41 communist_manifesto parallel
```

第一命中已经回到 `mes01.pdf` 第 451 PDF 页，即《共产党宣言》正文结尾。

验证：

```text
venv\Scripts\python.exe -m unittest tests.test_app_local_paths tests.test_page_metadata_inference
Ran 31 tests
OK

venv\Scripts\python.exe -m py_compile rag\exact_quote_lookup.py tests\test_app_local_paths.py
OK

venv\Scripts\python.exe scripts\evaluate_retrieval.py
28 evaluation cases completed
negative cases suppressed as expected
```

## 10. 当前下一步

当前 `mea01` - `mea10` 与 `mes01` - `mes04` 的核心库已重建并通过基础评测。

下一步不建议继续盲目 OCR，而应做小批量人工验收：

1. 抽 20-30 个真实问题，覆盖书目查询、精确引文、概念解释。
2. 检查回答里的卷名、文章名、`printed_page/pdf_page` 是否符合预期。
3. 对抽查中出现的错误页码或文章边界，回到对应 OCR cache 做局部修正。
4. 抽查通过后，再决定是否扩展到更多卷或进入前端问答体验调试。

## 11. 概念类检索降噪

第一轮人工验收抽查结果：

- 书目查询正常。
- 精确引文查询正常。
- `“全世界无产者，联合起来！”` 已优先返回 `mes01.pdf` 第 451 PDF 页。
- 概念类问题能召回正文，但偶尔混入索引页、注释页、目录式片段和 OCR 噪声标题。

修复：

- 在 `app.py` 的 rerank 阶段增加 `score_document_quality()`。
- 对以下候选降权：
  - `page_type=toc/title_page`
  - 标题或正文含“目录/目次/索引/注释/编者注”
  - 正文过短
  - 标点、破折号、页码串密度过高的索引式片段
  - 长标题但不像真实篇名的 OCR 噪声标题
- 增加测试：
  - `test_retrieve_documents_demotes_index_like_chunks`

验证：

```text
venv\Scripts\python.exe -m unittest tests.test_app_local_paths tests.test_page_metadata_inference
Ran 32 tests
OK
```

手工 trace-only 验证：

```text
什么是历史唯物主义？
```

降噪后，原先排第 2 的索引条目被压下，前五条主要回到正文阐述页：

```text
1 mes04.pdf 275
2 mes02.pdf 31
3 mes01.pdf 401
4 mes03.pdf 811
5 mes03.pdf 812
```

下一步继续跑总评测，并做一组概念题抽查，确认降噪没有误伤书目/引文链路。

## 12. 修正 `mea` / `mes` 书名映射

用户纠正：

- `mea*.pdf` 是《马克思恩格斯文集》。
- `mes*.pdf` 是《马克思恩格斯选集》。
- 之前写成“全集补卷”和“专题资料”是错误映射。

已修正：

- `rag/build_vectorstore_from_cache.py`
  - `mea01` - `mea10` -> 《马克思恩格斯文集》第 1-10 卷
  - `mes01` - `mes04` -> 《马克思恩格斯选集》第 1-4 卷
- `rag/exact_quote_lookup.py`
  - 精确引文兜底书名同样改为“文集/选集”
  - 对 `mea/mes` 优先使用文件名前缀映射，避免旧 OCR cache 里的旧 `book_title` 污染输出
- `app.py`
  - `normalize_book_parts()` 按 `mea/mes` 文件名前缀纠正旧向量库 metadata
  - `series_from_metadata()` 按 `mea/mes` 文件名前缀返回“文集/选集”
- `rag/article_map_core.json`
  - 书目表中的旧书名批量替换为“文集/选集”
- `vectorstore/marx_reader_core/index.pkl`
  - 对 42046 个 chunk 做 metadata-only 书名修复
  - 只更新 `book` 字段，不改页码、文章名和 chunk 内容
  - 备份：`vectorstore/marx_reader_core/index.pkl.bookfix2.20260518-154050.bak`
- `tests/test_app_local_paths.py`
  - 增加 `mea/mes` 按 source 纠正为“文集/选集”的回归测试

注意：

现有 `vectorstore/marx_reader_core` 的底层 metadata 已做书名字段修复；同时运行时 `normalize_metadata()` 仍保留按 source 纠正的兜底，避免旧缓存或旧索引再次污染输出。

## 13. 清洗概念检索中的噪声篇名

概念类 trace-only 抽查时发现：

- 页码和正文召回基本可用。
- 部分 `article/title_candidate` 来自 OCR 目录点线或正文残句，会污染引用显示。

典型问题：

```text
私有财产和共产主义].........................
2.价值增殖过程.........….......…
Yy 3uc
523
几乎又被《批判史》中以“历史眼光的广博远大”自诩的无知所超
```

修复：

- 在 `app.py` 增加 `is_noisy_article_title()`。
- `clean_article_title()` 会截断目录点线、去掉尾部残留括号和引号。
- 明显不可信的篇名不再用于显示和引用，但保留到 `raw_article/raw_section` 方便后续排查。
- 对“写于某年某月”、纯数字、ASCII 噪声、正文残句式标题做降级。

验证样例：

```text
人的本质是什么？
article=私有财产和共产主义

剩余价值是什么？
article=2.价值增殖过程
article=None  # 原 Yy 3uc

国家是怎么产生的？
article=None  # 原 523
article=None  # 原正文残句标题
```

测试：

```text
venv\Scripts\python.exe -m unittest tests.test_app_local_paths tests.test_page_metadata_inference
Ran 34 tests
OK
```

## 14. 概念检索贴题度加权

第二轮概念题抽查重点从“标题是否干净”转向“Top chunks 是否贴题”。

发现：

- `劳动过程是什么？` 会被“劳动对资本的形式上的从属”等相邻主题抢位。
- `资本是什么？` 曾被误走 exact quote，随后又被“自在的资本/地租”等片段抢到定义片段前面。
- `私有制与家庭起源有什么关系？` 应优先返回《家庭，私有制和国家的起源》正文。

修复：

- `retrieve_documents()` 只在真正的引文/出处查询时运行 `exact_quote_lookup()`。
  - 普通概念题不再因为短句碰巧在 OCR 中出现而被当成精确引文。
- 增加 `CONCEPT_FOCUS_TERMS` 与 `score_concept_focus()`。
  - 对问题中的核心概念词加权。
  - 篇名或正文前 300 字出现核心概念时上浮。
  - 对常见核心著作/章节标记加权，如《家庭，私有制和国家的起源》、劳动过程和价值增殖过程、自然辩证法等。
- 对 `X 是什么？` 类问题增加定义句优先级。
  - `什么是 X` / `X 是什么` 权重大于普通 `X 是...`。

验证样例：

```text
劳动过程是什么？
1 mes02.pdf 第191 PDF页，第五章劳动过程和价值增殖过程

资本是什么？
1 mea01.pdf 第151 PDF页，资本的利润
  preview: 什么是资本？“一定量的积蓄的和储存的劳动。”

私有制与家庭起源有什么关系？
1 mea04.pdf 第33 PDF页，家庭，私有制和国家的起源
2 mea04.pdf 第41 PDF页，家庭，私有制和国家的起源
```

测试：

```text
venv\Scripts\python.exe -m unittest tests.test_app_local_paths tests.test_page_metadata_inference
Ran 37 tests
OK
```

## 15. 检索评估脚本自动化判定

下一步把 `scripts/evaluate_retrieval.py` 从“人工看候选”推进到“自动给通过率”：

- 新增 `EvalResult` 汇总结构。
- `core_title`：Top1 必须命中 core classic 首选 source 和页码区间。
- `core_quote`：Top1 必须是 `match_type=exact_quote` 且 `confidence >= 1.0`。
- `negative`：必须没有可信 exact quote，保持 suppress。

验证结果：

```text
venv\Scripts\python.exe scripts\evaluate_retrieval.py
Passed: 28/28
core_title: 16/16
core_quote: 10/10
negative: 2/2
All evaluation cases passed.

venv\Scripts\python.exe -m unittest tests.test_app_local_paths tests.test_page_metadata_inference
Ran 37 tests
OK
```

## 16. 应用入口 smoke

用 `MARXOS_TRACE_ONLY=1` 走 `app.run_query()`，不调用 DeepSeek，只检查应用层路由、引用格式和 Top chunks。

结果：

- 《共产党宣言》书目查询：返回《马克思恩格斯选集》第1卷 376-435 页，和《马克思恩格斯文集》第2卷 3-67 页。
- “全世界无产者，联合起来！”：Top1 为 `mes01.pdf`，PDF 第451页；Top2 为 `mea02.pdf`，PDF 第86页。
- “资本是什么？”：Top1 为 `mea01.pdf`，《资本的利润》，PDF 第151页，预览命中“什么是资本？”。
- “劳动过程是什么？”：Top1 为 `mes02.pdf`，《第五章劳动过程和价值增殖过程》，PDF 第191页。
- “私有制与家庭起源有什么关系？”：Top1/Top2 为 `mea04.pdf`，《家庭，私有制和国家的起源》，PDF 第33/41页。

说明：PowerShell 捕获输出仍会显示 mojibake，但 trace 中的 source/page/series 逻辑正确。

## 17. 真实生成层 smoke 与输出风格收紧

不开 `MARXOS_TRACE_ONLY`，真实调用 DeepSeek：

- 书目查询和精确引文查询不依赖 LLM，端到端直接返回正确出处。
- 概念解释和理论分析在沙箱内会出现 `APIConnectionError`，放开网络后可正常生成。
- 首轮真实生成发现模型会输出问候、自称、结尾邀约，并可能带出“【原著内容】”和 `(1)` 这类内部上下文编号。

修复：

- `app.py` 新增 `final_answer_style_rules()`，统一约束 quote/concept/analysis/default prompt。
- 最终回答禁止问候、自称、结尾邀约。
- 最终回答禁止输出“【原著内容】”“【检索材料】”“CTX-1”等内部栏目名。
- `build_context()` 的“句子引用格式/段落具体出处格式”不再带 `(1)` chunk 编号。
- 对不成对书名号的 OCR 篇名做噪声过滤，例如 `资本论》第三册增补` 不再进入 citation article。

验证：

```text
venv\Scripts\python.exe -m unittest tests.test_app_local_paths tests.test_page_metadata_inference
Ran 38 tests
OK

venv\Scripts\python.exe scripts\evaluate_retrieval.py
Passed: 28/28
core_title: 16/16
core_quote: 10/10
negative: 2/2
All evaluation cases passed.
```

## 19. 扩展检索评测集

将 `scripts/evaluate_retrieval.py` 从 28 条手工基线扩展为 93 条稳定基线：

- 保留原有手工 title / quote / negative 用例。
- 从 `rag/core_classics.json` 自动生成核心经典标题定位问题，覆盖正题名、别名和页码范围问法。
- 从核心经典 quote 列表自动生成精确引文定位问题。
- 新增概念检索用例，直接调用 `app.retrieve_documents()`，覆盖“资本是什么？”、“劳动过程是什么？”、“私有制与家庭起源有什么关系？”、“阶级斗争是什么意思？”、“国家的起源是什么？”、“剩余价值是什么？”、“异化劳动是什么？”、“费尔巴哈提纲讲实践吗？”等。
- 新增伪造标题和伪造引文负例，确认不会误报 exact quote。
- 为宽概念检索增加权威来源/篇名/页码范围偏好，概念问题会扩大候选池后再重排。
- 对书信、索引、目录、准备材料、补充和修改等材料作降权，避免宽概念问题被弱相关页抢占。

当前稳定结果：

```text
venv\Scripts\python.exe scripts\evaluate_retrieval.py
Passed: 93/93
core_title: 57/57
core_quote: 19/19
negative: 8/8
concept: 9/9
All evaluation cases passed.
```

扩展过程中也暴露出一批宽概念问题，其中多数已通过概念 rerank 修复。暂不纳入硬性通过集的问题：

- “商品价值如何形成？”

## 20. 精确引文正文页优先

修复“国家是社会在一定发展阶段上的产物。”优先命中《马克思恩格斯选集》第4卷注释页的问题。

原因：

- `exact_quote_lookup()` 先扫描核心经典范围，若 scoped 命中就不再进入 global fallback。
- 该引文在《马克思恩格斯选集》第4卷注释页完整出现，同时正文页也存在，但正文页需要全局候选才能进入排序。
- 结果导致注释性说明页压过正文页。

处理：

- 精确引文检索合并 scoped 和 global 候选后统一排序。
- `hit_quality_rank()` 对“重要著作”“选编说明”“编者注”等注释/说明页降权。
- 新增回归测试 `test_exact_quote_prefers_body_hit_over_annotation`。

验证：

```text
“国家是社会在一定发展阶段上的产物。” Top1:
source=mea04.pdf, pdf_page=206

venv\Scripts\python.exe -m unittest tests.test_app_local_paths tests.test_page_metadata_inference
Ran 40 tests
OK

venv\Scripts\python.exe scripts\evaluate_retrieval.py
Passed: 93/93
core_title: 57/57
core_quote: 19/19
negative: 8/8
concept: 9/9
All evaluation cases passed.
```

## 21. 精确引文 Top1 注释页审计

新增 `scripts/audit_exact_quote_top1.py`，用于批量检查核心引文 Top1 是否落到目录、索引、注释、编者注、说明、序言、选编说明等非正文页。

审计策略：

- 读取 `scripts.evaluate_retrieval.build_questions()` 中的 `core_quote` 用例。
- 对每条问题调用 `exact_quote_lookup(..., limit=3)`。
- 检查 Top1 的 article / section 元数据是否包含目录、索引、注释等标记。
- 检查 Top1 内容开头是否包含“重要著作”“本文节选自”“选编说明”“编者注”等说明性标记。

当前结果：

```text
venv\Scripts\python.exe scripts\audit_exact_quote_top1.py
Audited core_quote Top1: 19
No annotation-like Top1 hits found.
```

真实生成复测“资本是什么？”：

```text
has_greeting False
has_internal_label False
has_numbered_citation False
has_followup False
```

## 18. CLI 入口 smoke 与 PowerShell 编码保护

下一步检查 `app.py` 命令行入口时，发现 PowerShell 管道输入中文会在某些环境下变成 `????????`。这类输入已经不可逆，如果继续跑检索，会误入向量检索甚至误调用 DeepSeek。

修复：

- `app.py` 新增 `is_unreadable_query()`。
- `run_query()` 开头先清理输入；如果检测到不可读输入，直接返回编码提示，不加载向量库、不调用 DeepSeek。
- README 的旧调试变量 `MARXOS_DEBUG_SOURCES` 更新为当前实际使用的：
  - `MARXOS_DEV_MODE=1`
  - `MARXOS_TRACE=1`
  - `MARXOS_TRACE_ONLY=1`

验证：

```text
PowerShell 管道中文输入被替换为问号时：
未能读取到可用的中文问题。如果是在 PowerShell 中通过管道或重定向输入，请先运行 `chcp 65001`，或在交互式提示中直接输入问题。

venv\Scripts\python.exe -m unittest tests.test_app_local_paths tests.test_page_metadata_inference
Ran 39 tests
OK

venv\Scripts\python.exe scripts\evaluate_retrieval.py
Passed: 28/28
core_title: 16/16
core_quote: 10/10
negative: 2/2
All evaluation cases passed.
```
