# MarxOS 系统全景架构

```
═══════════════════════════════════════════════════════════════════════════════════════════════════
                                      MarxOS — 马克思经典文本智能问答系统
                                          System Architecture Overview
═══════════════════════════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                         📱 用户界面层                                            │
│                                                                                                 │
│   ┌──────────────────────────┐                                        ┌──────────────────────┐ │
│   │       Web 应用            │                                        │     CLI 命令行        │ │
│   │   http://127.0.0.1:7860  │                                        │                      │ │
│   │                          │                                        │  $ python app.py     │ │
│   │  ┌────────────────────┐ │                                        │    "什么是异化劳动"    │ │
│   │  │ 智能 │ 精确问答 │ 深度分析 │ │                                        │                      │ │
│   │  └────────────────────┘ │                                        │                      │ │
│   │  ┌────────────────────┐ │                                        │                      │ │
│   │  │    聊天区 + 证据卡   │ │                                        │                      │ │
│   │  └────────────────────┘ │                                        │                      │ │
│   └──────────────────────────┘                                        └──────────────────────┘ │
└──────────────────────────────────────┬──────────────────────────────────────────────────────────┘
                                       │
                                       │  POST /api/ask  │  app.run_query()
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                      🧠 编排调度层                                                │
│                                                                                                 │
│   ┌─────────────────────────────────────────┐  ┌──────────────────────────────────────────────┐ │
│   │          Query Pipeline                  │  │           Quality Control                     │ │
│   │                                         │  │                                              │ │
│   │  ① 意图识别     classify_query()         │  │  ⑤ CRAG 评分    assess_retrieval_quality()   │ │
│   │     7 种意图    (marxos_query_intent)     │  │     阈值 45-55   ──→ 纠正性重新检索 (≤2轮)    │ │
│   │          │                              │  │                                              │ │
│   │          ▼                              │  │  ⑥ 引文审计    audit_answer_citations()      │ │
│   │  ② 约束构建   constraints_from_query()   │  │     格式检查 + OCR 内容验证                   │ │
│   │     WorkCatalog → Locator → 概念 → 主题   │  │     ──→ 恢复循环 (≤2轮)                      │ │
│   │          │                              │  │                                              │ │
│   │          ▼                              │  └──────────────────────────────────────────────┘ │
│   │  ③ 检索       retrieve_documents()       │                                                  │
│   │     Dense + BM25 → RRF 融合               │  ┌──────────────────────────────────────────────┐ │
│   │     小块检索 (180 char)                    │  │           External APIs                      │ │
│   │     ──→ 段落窗口扩展 (1-3 段)              │  │                                              │ │
│   │          │                              │  │  ┌──────────┐  ┌───────────────────────────┐ │ │
│   │          ▼                              │  │  │ DeepSeek │  │  OpenAI-compatible API     │ │ │
│   │  ④ 生成       build_prompt(intent)       │  │  │   Chat   │  │  deepseek-chat             │ │ │
│   │     ──→ Evidence Cards                   │  │  │          │  │  max_retries=3              │ │ │
│   │     ──→ DeepSeek API                     │  │  └──────────┘  └───────────────────────────┘ │ │
│   └─────────────────────────────────────────┘  └──────────────────────────────────────────────┘ │
└──────────────────────────────────────┬──────────────────────────────────────────────────────────┘
                                       │
                    ┌──────────────────┼──────────────────┐
                    ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                     🗄️ 检索引擎层                                                │
│                                                                                                 │
│   ┌──────────────────────────┐  ┌──────────────────────────┐  ┌──────────────────────────────┐ │
│   │    Milvus (V3 主引擎)     │  │    FAISS (V2 / 离线)     │  │    BM25 (稀疏检索)            │ │
│   │                          │  │                          │  │                              │ │
│   │  Collection:              │  │  vectorstore/             │  │  paragraph_cache_core.jsonl  │ │
│   │   marxos_me_passages      │  │   marx_reader_core/       │  │                              │ │
│   │                          │  │                          │  │  关键词匹配 + 得分            │ │
│   │  Index: HNSW              │  │  Index: IVF              │  │  top_k = 24                  │ │
│   │  Metric: COSINE           │  │  Metric: L2              │  │                              │ │
│   │  Dim: 1024 (BGE-M3)       │  │  Dim: 384 (MiniLM)       │  │  角色: Dense 语义补充          │ │
│   │                          │  │                          │  │                              │ │
│   │  角色: 主检索              │  │  角色: 离线 / fallback     │  │                              │ │
│   └──────────────────────────┘  └──────────────────────────┘  └──────────────────────────────┘ │
│                                                                                                 │
│                         VectorBackend Protocol (marxos_vector_backend.py)                        │
│                         ┌────────────────────────────────────────┐                               │
│                         │  search(query, k, filters) → [Result]  │                               │
│                         └────────────────────────────────────────┘                               │
└──────────────────────────────────────┬──────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                     📊 数据资产层                                                │
│                                                                                                 │
│   ┌────────────────────┐  ┌────────────────────┐  ┌────────────────────┐  ┌─────────────────┐  │
│   │  OCR 文本缓存        │  │  段落缓存            │  │  结构化知识库        │  │  评估数据集       │  │
│   │                    │  │                    │  │                    │  │                 │  │
│   │  data/ocr_cache/   │  │  paragraph_cache    │  │  work_catalog.json │  │  eval_dataset   │  │
│   │  75 卷 × 57K 页     │  │  _core.jsonl       │  │  89 部著作元数据     │  │  _v2 (200题)    │  │
│   │                    │  │                    │  │                    │  │  _me_200 (200)  │  │
│   │  page_*.json        │  │  43212 条段落        │  │  article_locators  │  │                 │  │
│   │  page_*.txt         │  │  带 parent_id       │  │  147K 行            │  │  intent_dataset │  │
│   │                    │  │  页码 + 来源         │  │  letter_locators   │  │  _10000 (10K)   │  │
│   │  角色: 原始文本       │  │                    │  │  79K 行             │  │                 │  │
│   │                    │  │  角色: 检索中间层      │  │                    │  │  角色: 质量评估   │  │
│   │                    │  │                    │  │  角色: 精确约束      │  │                 │  │
│   └────────────────────┘  └────────────────────┘  └────────────────────┘  └─────────────────┘  │
│                                                                                                 │
│   ┌────────────────────┐  ┌────────────────────┐  ┌────────────────────────────────────────┐   │
│   │  Embedding 模型      │  │  LLM 服务            │  │  辅助数据                               │   │
│   │                    │  │                    │  │                                        │   │
│   │  BGE-M3 (1024-dim) │  │  DeepSeek Chat     │  │  page_map.json (PDF→印刷页码)          │   │
│   │  MiniLM (384-dim)  │  │  temperature=0     │  │  intent_classifier.pkl (23KB)          │   │
│   │                    │  │  max_tokens=300    │  │  article_map_core.json (TOC)           │   │
│   │  normalize=True    │  │                    │  │                                        │   │
│   └────────────────────┘  └────────────────────┘  └────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════════════════════════
                                     📐 核心设计决策
═══════════════════════════════════════════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────────────────────────────────────┐
  │                                   小块检索 → 大块召回                                      │
  │                                                                                         │
  │   文档 ──→ 段落检测 ──→ 切小块 (180 char) ──→ Embedding ──→ FAISS/Milvus               │
  │                                          ▲                                              │
  │                                          │  parent_paragraph_id                         │
  │                                          │                                              │
  │   用户 Query ──→ 小块检索 ──→ 命中 child chunk ──→ 查 parent_paragraph_id              │
  │                                                   ──→ 取前1段 + 本段 + 后1段            │
  │                                                   ──→ 500-2000 char 上下文              │
  │                                                                                         │
  │   优势: 小块 Embedding 语义精准 + 大窗口上下文完整 + 引文页码精确                           │
  └──────────────────────────────────────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────────────────────────────────────┐
  │                                   7 类意图路由引擎                                         │
  │                                                                                         │
  │   查询 ──→ 规则评分 (7维) ──→ 归一化 ──→ 最高分意图                                       │
  │            ▲                                                                             │
  │            ├─ bibliographic_lookup  书目查找  "《X》在哪一卷"                              │
  │            ├─ quote_lookup          引文定位  "XX出自哪里"                                │
  │            ├─ concept_explain       概念解释  "什么是XX"                                  │
  │            ├─ comparison            比较分析  "比较A和B"                                  │
  │            ├─ deep_analysis          深度分析  "运用马克思主义分析XX"                      │
  │            ├─ theory_analysis        理论分析  "如何理解XX"                                │
  │            └─ rag_answer            通用问答  兜底                                        │
  │                                                                                         │
  │   增强: ML 分类器 (LR on BGE-M3, 23KB) 混合评分 — 训练中                                  │
  └──────────────────────────────────────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────────────────────────────────────┐
  │                                   自修复质量闭环                                           │
  │                                                                                         │
  │   检索 ──→ 评分 (<55) ──→ 🔄 纠正性检索 ──→ 评分 (<55) ──→ 🔄 第2轮纠正                   │
  │     │                         │                         │                                │
  │     └── 评分 ≥55 ──→ ✅ ──→ LLM 生成                    └── ≥55 ──→ ✅                   │
  │                                                                                         │
  │   生成 ──→ 引文审计 ──→ 格式 + OCR 验证 ──→ 不通过 ──→ 🔄 重新生成 (≤2轮)                 │
  └──────────────────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════════════════════════
                                     📦 技术栈
═══════════════════════════════════════════════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────────────────────────────────────────┐
  │                                                                                         │
  │   层次          │  组件               │  版本        │  角色                               │
  │  ───────────────┼────────────────────┼─────────────┼─────────────────────────────────── │
  │   向量库         │  Milvus             │  2.5.x       │  主检索引擎 (HNSW + COSINE)         │
  │   向量库 (离线)  │  FAISS              │  1.7.4       │  离线 fallback (IVF + L2)            │
  │   Embedding     │  BGE-M3             │  1024-dim    │  主模型 (中文优化, normalize)         │
  │   Embedding     │  MiniLM-L12-v2      │  384-dim     │  离线 fallback                       │
  │   LLM           │  DeepSeek Chat      │  API         │  答案生成                            │
  │   OCR           │  PaddleOCR          │  2.7.3       │  PDF → 文本                          │
  │   文本分割       │  LangChain Splitter │  0.0.2       │  RecursiveCharacterTextSplitter       │
  │   关键词检索     │  BM25               │  rank-bm25   │  稀疏检索补充                         │
  │   意图分类       │  Jieba + LR         │  0.42 + skl  │  规则 + ML 混合                       │
  │   Web 服务       │  http.server        │  stdlib      │  自定义 HTTP + SPA                    │
  │   可观测         │  Phoenix/OTEL       │  可选        │  LLM 追踪                            │
  │                                                                                         │
  └──────────────────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════════════════════════
                                     📁 项目结构
═══════════════════════════════════════════════════════════════════════════════════════════════════

  MarxOS/
  ├── app.py                        主入口 (run_query, classify_query, build_context)
  ├── web_app.py                    Web 服务 (HTTP + SPA)
  │
  ├── marxos_query_intent.py        意图识别 (7类评分 + ML混合)
  ├── marxos_orchestration.py       编排调度 (检索→CRAG→生成→审计)
  ├── marxos_query_planner.py       查询规划 (分解/HyDE/多查询)
  ├── marxos_prompts.py             Prompt 模板 (5种)
  ├── marxos_citations.py           引文格式化 + 审计
  ├── marxos_answers.py             本地规则回答 (无需 LLM)
  ├── marxos_ambiguous.py           歧义查询处理
  ├── marxos_runtime.py             FAISS 运行时状态
  ├── marxos_vector_backend.py      Milvus/FAISS 抽象层
  ├── marxos_embeddings.py          Embedding 模型适配
  ├── marxos_work_catalog.py        著作目录匹配 (89部)
  ├── marxos_book_locator.py        LLM 著作定位
  ├── marxos_citation_verifier.py   LLM 引文验证
  ├── marxos_phoenix.py             可观测追踪
  ├── marxos_intent_classifier.py   ML 意图分类器
  │
  ├── retrieval/                    检索子系统
  │   ├── constraints.py            约束构建 (WorkCatalog→Locator→概念→主题)
  │   ├── modes.py                  检索模式 (Dense + BM25 + Hybrid)
  │   └── ranking.py                重排序 / 去重 / 多样化
  │
  ├── rag/                          RAG 数据管线
  │   ├── ocr_to_cache.py           PDF → OCR → JSON/TXT
  │   ├── clean_ocr_text.py         OCR 清洗
  │   ├── paragraph_cache.py        段落检测 → JSONL
  │   ├── build_vectorstore_from_cache.py  [已废弃] v1 FAISS 构建
  │   ├── semantic_retrieval.py     语义检索 (BM25 + 段落窗口)
  │   ├── exact_quote_lookup.py     精确引文 OCR 查找
  │   └── work_catalog.json         89部著作结构化元数据
  │
  ├── scripts/                      工具脚本 (46个)
  │   ├── build_paragraph_cache.py
  │   ├── build_semantic_child_vectorstore.py
  │   ├── build_paragraph_vectorstore.py
  │   ├── build_milvus_collection.py    [新] Milvus 构建
  │   ├── build_intent_classifier.py    [新] ML 分类器训练
  │   ├── evaluate_e2e.py
  │   ├── audit.py
  │   ├── check.py
  │   └── ...
  │
  ├── tests/                        测试 (8文件, 91方法)
  ├── data/                         数据资产 (gitignored)
  │   ├── ocr_cache/                75卷 × 57K 页
  │   ├── paragraph_cache_core.jsonl 43K 段落
  │   ├── page_map.json             PDF↔印刷页码
  │   └── intent_classifier.pkl     ML 模型
  │
  └── vectorstore/                  向量库 (gitignored)
      ├── marx_reader_core/         子块 FAISS
      └── marx_reader_paragraph/    段落 FAISS

═══════════════════════════════════════════════════════════════════════════════════════════════════
                                     🔄 数据流向
═══════════════════════════════════════════════════════════════════════════════════════════════════

  ① 数据摄入                        ② 知识构建                         ③ 查询服务
  ───────────                       ──────────                         ──────────

  PDF 原著                          段落缓存                           用户 Query
  (75卷)                            paragraph_cache                       │
     │                              _core.jsonl                     ┌─────┴─────┐
     │ PaddleOCR                        │                           │ 规则 + ML  │
     ▼                                  │                           │ 意图识别    │
  OCR 缓存                ┌─────────────┼─────────────┐             └─────┬─────┘
  page_*.json              │             │             │                   │
  page_*.txt               ▼             ▼             ▼                   ▼
     │                 子块向量库    段落向量库     Milvus            约束构建
     │                 FAISS         FAISS        Collection          (5层递进)
     │                 180 char      完整段落      结构化Schema            │
     │                 298K 条       43K 条        (构建中)               ▼
     │                     │             │             │              检索
     └─────────────────────┴─────────────┴─────────────┘          Dense + BM25
                                                                     │
                                                                     ▼
                                                               段落窗口扩展
                                                               (小块→大块)
                                                                     │
                                                                     ▼
                                                               Evidence Cards
                                                                     │
                                                                     ▼
                                                               DeepSeek 生成
                                                                     │
                                                                     ▼
                                                               引文审计 + 输出
